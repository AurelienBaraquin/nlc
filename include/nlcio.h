#pragma once

#define NULL ((void *)0)

// print
int putchar(int c);
int puts(const char *s);
int printf(const char *format, ...);

// scan
int getchar();
int gets(char *s);
int scanf(const char *format, ...);
